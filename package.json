{
  "name": "quotatide",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Build and release tooling for the QuotaTide Rust/Tauri desktop app",
  "author": "TheBlind",
  "license": "MIT",
  "repository": "https://github.com/TheBlindM/QuotaTide",
  "scripts": {
    "icons": "node scripts/generate-icons.mjs",
    "test": "node --test scripts/*.test.mjs scripts/release/*.test.mjs scripts/qa/*.test.mjs",
    "check": "npm test && node scripts/check-desktop-versions.mjs && node scripts/check-desktop-identity.mjs && node scripts/check-updater-key.mjs"
  },
  "engines": {
    "node": ">=22.13"
  }
}
