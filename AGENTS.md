## Agent skills

### Issue tracker

Issues 使用 `.scratch/<feature-slug>/` 下的 Local Markdown 文件管理。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认分诊标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

采用 single-context 结构：根目录 `CONTEXT.md` 与 `docs/adr/`。详见 `docs/agents/domain.md`。
